# Research — <feature/slug>

> Phase 1. Understand the current state before proposing a change. Cite real code (`file:line`).

## Goal / problem
<What are we trying to achieve, in one or two sentences? Link the issue/PR if any.>

## Area & governing guide
- Primary area(s): `apps/api/internal/…` / `apps/api/cmd/` / `apps/web/`
- Nested guide(s) to follow: <e.g. `apps/api/internal/storage/AGENTS.md`>

## What exists today
- <Relevant types/interfaces/files and what they do — cite `path:line`.>
- <How the closest existing implementation works (the pattern to mirror).>

## Constraints & considerations
- <Streaming? Context propagation? Config + DB + web touchpoints? Security/secrets?>

## Prior art in the repo
- <Similar past changes, commits, or specs to learn from.>
